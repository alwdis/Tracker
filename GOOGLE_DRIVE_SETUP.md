# 🔐 Настройка Google Drive API для облачной синхронизации

## ⏱️ Время выполнения: ~15-20 минут

---

# 📋 ЧАСТЬ 1: Создание проекта в Google Cloud Console

## Шаг 1.1: Открыть Google Cloud Console

1. **Перейдите** по ссылке: https://console.cloud.google.com/
2. **Войдите** под своим Google аккаунтом
3. **Согласитесь** с условиями использования (если это первый раз)

---

## Шаг 1.2: Создать новый проект

1. **Нажмите** на выпадающий список проектов
   - Он находится вверху страницы, слева от "Google Cloud"
   - Может быть написано "Select a project" или название существующего проекта

2. **В появившемся окне** нажмите `NEW PROJECT` (вверху справа)

3. **Заполните форму**:
   ```
   Project name: Tracker App
   (или любое другое название)
   
   Location: No organization
   (оставить по умолчанию)
   ```

4. **Нажмите** кнопку `CREATE`

5. **Подождите** ~30 секунд, пока проект создастся
   - В правом верхнем углу появится уведомление
   - Нажмите на уведомление или выберите проект из списка

---

## Шаг 1.3: Включить Google Drive API

1. **Убедитесь**, что выбран ваш новый проект
   - Проверьте название вверху страницы

2. **Откройте меню** (три горизонтальные линии ☰ вверху слева)

3. **Найдите** `APIs & Services` → **Выберите** `Library`
   ```
   Прямая ссылка:
   https://console.cloud.google.com/apis/library
   ```

4. **В поиске** (вверху страницы) введите:
   ```
   Google Drive API
   ```

5. **Нажмите** на `Google Drive API` в результатах поиска

6. **Нажмите** синюю кнопку `ENABLE` (Включить)

7. **Подождите** ~10 секунд
   - Страница перезагрузится
   - API теперь включен ✅

---

# 📋 ЧАСТЬ 2: Настройка OAuth Consent Screen

## Шаг 2.1: Перейти к настройкам

1. **Откройте меню** (☰)
2. **Выберите**: `APIs & Services` → `OAuth consent screen`
   ```
   Прямая ссылка:
   https://console.cloud.google.com/apis/credentials/consent
   ```

---

## Шаг 2.2: Выбрать тип приложения

Вы увидите два варианта:

```
○ Internal (Внутреннее)
  Только для пользователей вашей организации
  
● External (Внешнее)  ← ВЫБРАТЬ ЭТОТ
  Для любых пользователей с Google аккаунтом
```

1. **Выберите** `External`
2. **Нажмите** `CREATE`

---

## Шаг 2.3: Заполнить App Information (Экран 1 из 4)

### Обязательные поля:

```
App name: Tracker
(Название вашего приложения - может быть любым)

User support email: ваш@gmail.com
(Выберите из выпадающего списка свой email)
```

### Опциональные поля (можно пропустить):

```
App logo: (пропустить)
Application home page: (пропустить)
Application privacy policy link: (пропустить)
Application terms of service link: (пропустить)
```

### Внизу страницы:

```
Developer contact information:
Email addresses: ваш@gmail.com
(Тот же email, что и выше)
```

**Нажмите** `SAVE AND CONTINUE`

---

## Шаг 2.4: Добавить Scopes (Экран 2 из 4)

1. **Нажмите** `ADD OR REMOVE SCOPES`

2. **В появившемся окне**, в поиске введите:
   ```
   drive.file
   ```

3. **Найдите и отметьте галочкой**:
   ```
   ✅ .../auth/drive.file
   
   Описание: "View and manage Google Drive files 
   and folders that you have opened or created with this app"
   ```

4. **Прокрутите вниз** в этом окне и нажмите `UPDATE`

5. **Проверьте**, что scope добавлен в таблицу

6. **Нажмите** `SAVE AND CONTINUE`

---

## Шаг 2.5: Добавить Test Users (Экран 3 из 4)

⚠️ **ВАЖНО:** Пока приложение не опубликовано, работать будет только для тестовых пользователей!

### Для тестирования (сейчас):

1. **Нажмите** `+ ADD USERS`

2. **Введите** email адрес для тестирования:
   ```
   ваш@gmail.com
   (или email, с которого будете тестировать)
   ```

3. **Нажмите** `ADD`

4. **Можете добавить** еще email-ы (до 100):
   - Друзей для тестирования
   - Свои дополнительные аккаунты
   - И т.д.

5. **Нажмите** `SAVE AND CONTINUE`

---

### Для публичного использования (потом):

После тестирования вернитесь сюда и:

1. Перейдите в `OAuth consent screen`
2. Найдите кнопку `PUBLISH APP`
3. Нажмите и подтвердите
4. ✅ Теперь любой пользователь сможет подключиться!

---

## Шаг 2.6: Summary (Экран 4 из 4)

1. **Просмотрите** всю информацию
2. **Нажмите** `BACK TO DASHBOARD`

---

# 📋 ЧАСТЬ 3: Создание OAuth 2.0 Credentials

## Шаг 3.1: Перейти к Credentials

1. **Откройте меню** (☰)
2. **Выберите**: `APIs & Services` → `Credentials`
   ```
   Прямая ссылка:
   https://console.cloud.google.com/apis/credentials
   ```

---

## Шаг 3.2: Создать OAuth Client ID

1. **Нажмите** `+ CREATE CREDENTIALS` (вверху страницы)

2. **В выпадающем меню** выберите:
   ```
   OAuth client ID
   ```

3. **Application type** выберите:
   ```
   ● Desktop app  ← ВЫБРАТЬ ДЛЯ ELECTRON
   ```

4. **Name** введите:
   ```
   Tracker Desktop App
   (или любое другое имя)
   ```

5. **Нажмите** `CREATE`

---

## Шаг 3.3: 🎉 ПОЛУЧИТЬ КЛЮЧИ!

Появится окно с вашими ключами:

```
┌────────────────────────────────────────────────────┐
│  OAuth client created                              │
├────────────────────────────────────────────────────┤
│                                                    │
│  Your Client ID:                                   │
│  123456789012-abc...xyz.apps.googleusercontent.com │
│  [📋 Copy]                                         │
│                                                    │
│  Your Client Secret:                               │
│  GOCSPX-aBcDeFgHiJkLmNoPqRsTuVwXyZ                │
│  [📋 Copy]                                         │
│                                                    │
│  [DOWNLOAD JSON]  [OK]                             │
└────────────────────────────────────────────────────┘
```

### ⚠️ СОХРАНИТЕ ЭТИ КЛЮЧИ!

**Способ 1 (Рекомендуется):**
1. **Нажмите** `DOWNLOAD JSON`
2. **Сохраните** файл в безопасном месте
3. ⚠️ **НЕ ЗАГРУЖАЙТЕ** этот файл в Git/GitHub!

**Способ 2:**
1. **Скопируйте** `Client ID` → вставьте в блокнот
2. **Скопируйте** `Client Secret` → вставьте в блокнот
3. **Сохраните** файл в безопасном месте

**Нажмите** `OK`

---

# 📋 ЧАСТЬ 4: Настройка в приложении

## Шаг 4.1: Открыть файл electron.js

1. Откройте файл: `public/electron.js`

2. **Найдите** секцию (примерно строка ~130):

```javascript
// Google Drive API Configuration
const GOOGLE_DRIVE_CONFIG = {
  clientId: process.env.GOOGLE_CLIENT_ID || 'YOUR_CLIENT_ID_HERE',
  clientSecret: process.env.GOOGLE_CLIENT_SECRET || 'YOUR_CLIENT_SECRET_HERE',
  redirectUri: 'http://localhost',
  scopes: ['https://www.googleapis.com/auth/drive.file']
};
```

## Шаг 4.2: Вставить свои ключи

**Замените** `YOUR_CLIENT_ID_HERE` и `YOUR_CLIENT_SECRET_HERE` на свои значения:

```javascript
// Google Drive API Configuration
const GOOGLE_DRIVE_CONFIG = {
  clientId: '123456789012-abc...xyz.apps.googleusercontent.com',  // ← Ваш Client ID
  clientSecret: 'GOCSPX-aBcDeFgHiJkLmNoPqRsTuVwXyZ',              // ← Ваш Client Secret
  redirectUri: 'http://localhost',
  scopes: ['https://www.googleapis.com/auth/drive.file']
};
```

**Сохраните** файл (Ctrl+S)

---

# 📋 ЧАСТЬ 5: Тестирование

## Шаг 5.1: Пересобрать приложение

```bash
npm run build
```

## Шаг 5.2: Запустить приложение

```bash
npm start
```

## Шаг 5.3: Протестировать авторизацию

1. **Откройте** приложение
2. **Нажмите** на иконку облака (Cloud Sync)
3. **Выберите** вкладку `Google Drive`
4. **Нажмите** `Подключить`
5. **Откроется браузер** с формой авторизации Google
6. **Войдите** под своим Google аккаунтом (тестовым)
7. **Разрешите** доступ к Google Drive
8. **Приложение автоматически** получит доступ
9. **Проверьте**: статус должен стать "Подключено" ✅

## Шаг 5.4: Протестировать синхронизацию

1. **Нажмите** `Синхронизация на облако` (↑)
2. **Подождите** ~2-5 секунд
3. **Проверьте**: должно появиться "Синхронизация завершена" ✅

4. **Проверьте** в Google Drive:
   - Зайдите на https://drive.google.com/
   - Папка `Tracker` → файл `tracker-backup.json`

---

# 🎯 Готово!

Теперь Google Drive настроен и работает! 🎉

---

# 📝 Дополнительная информация

## 🔒 Безопасность

### Для Desktop App (Electron):
- ✅ Client ID можно вшить в код (это публичная информация)
- ✅ Client Secret тоже можно вшить (для desktop app это нормально)
- 🔐 Настоящая безопасность - в том, что каждый пользователь авторизуется через СВОЙ Google аккаунт
- 🚫 Вы НЕ видите файлы пользователей
- 🚫 Вы НЕ имеете доступа к их данным

### Рекомендации:
- Добавьте в `.gitignore`:
  ```
  client_secret_*.json
  credentials.json
  ```

## 📊 Лимиты Google Drive API

### Бесплатно:
- ✅ 1,000,000,000 запросов в день (Queries per day)
- ✅ 10,000 запросов в 100 секунд на пользователя

### Это означает:
- Если у вас 1000 пользователей
- Каждый делает 10 синхронизаций в день
- Это всего 10,000 запросов/день
- Вы далеко от лимита! ✅

## 🌐 Публикация приложения (для публичного использования)

Когда будете готовы выпустить приложение публично:

### Шаг 1: Вернуться в Google Cloud Console
```
https://console.cloud.google.com/apis/credentials/consent
```

### Шаг 2: Опубликовать
1. Найдите статус: `Testing` 🟡
2. Нажмите `PUBLISH APP`
3. Подтвердите публикацию
4. Статус изменится на: `In production` 🟢

### Шаг 3: Готово!
Теперь ЛЮБОЙ пользователь с Google аккаунтом может:
- Скачать ваше приложение
- Нажать "Подключить Google Drive"
- Авторизоваться
- Использовать облачную синхронизацию ✅

---

## ❓ Частые вопросы

### Q: Нужно ли пользователям создавать свой API?
**A:** НЕТ! ❌ Вы создаёте API один раз, все пользователи используют ваши ключи.

### Q: Будут ли пользователи видеть файлы друг друга?
**A:** НЕТ! ❌ Каждый пользователь авторизуется через свой Google аккаунт и видит только свои файлы.

### Q: Смогу ли я (разработчик) видеть файлы пользователей?
**A:** НЕТ! ❌ У вас нет доступа к их Google Drive.

### Q: Что если я превышу лимиты API?
**A:** Для нормального использования лимиты ОЧЕНЬ большие (1 млрд запросов/день). Если вдруг превысите - Google отправит уведомление, можно увеличить квоту.

### Q: Безопасно ли хранить Client Secret в коде?
**A:** ДА! ✅ Для Desktop Application (OAuth 2.0 для native apps) это стандартная практика. Client Secret для desktop app не считается конфиденциальным.

### Q: Что делать, если забыл сохранить ключи?
**A:** Вернитесь в `APIs & Services` → `Credentials` → найдите свой OAuth 2.0 Client ID → нажмите на него → скопируйте ключи снова.

---

## 🆘 Помощь

Если что-то не работает:
1. Проверьте, что проект выбран правильный
2. Проверьте, что Google Drive API включен (зелёная галочка)
3. Проверьте, что OAuth consent screen настроен
4. Проверьте, что вы добавлены как Test User (если не опубликовано)
5. Проверьте, что ключи скопированы правильно (без пробелов)

---

# ✅ Чеклист

Отметьте, что сделано:

- [ ] Создан проект в Google Cloud Console
- [ ] Включен Google Drive API
- [ ] Настроен OAuth consent screen
- [ ] Добавлен scope `drive.file`
- [ ] Добавлены test users
- [ ] Созданы OAuth 2.0 Credentials (Web application)
- [ ] Скопированы Client ID и Client Secret
- [ ] Создан файл `.env` с ключами
- [ ] Пересобрано приложение (`npm run build`)
- [ ] Протестирована авторизация
- [ ] Протестирована синхронизация

## 7. Настройка переменных окружения

После получения ключей, создайте файл `.env` в корне проекта:

```bash
# Скопируйте env.example в .env
cp env.example .env
```

Затем отредактируйте `.env` файл и вставьте ваши ключи:

```env
GOOGLE_CLIENT_ID=ваш_client_id_здесь
GOOGLE_CLIENT_SECRET=ваш_client_secret_здесь
```

**⚠️ Важно**: Файл `.env` автоматически исключен из git и не будет загружен в репозиторий.

---

**Готово!** 🎉 Теперь все пользователи могут использовать Google Drive для облачной синхронизации!
